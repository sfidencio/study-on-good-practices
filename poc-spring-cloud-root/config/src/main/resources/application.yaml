spring:
  application:
    name: config

  cloud:
    config:
      server:
        git:
          uri: https://github.com/sfidencio/study-on-good-practices/tree/master/poc-spring-cloud-root/config
        bootstrap: true
server:
  port: 8888
